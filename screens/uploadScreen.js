import React, {useState} from 'react'
import {
	View, Text, TouchableOpacity, TextInput, ToastAndroid
} from 'react-native'

export default function UploadScreen({navigation}){

	console.log('a')

	return(
		<WebView
      		source={{ uri: 'https://expo.dev' }}
    	/>
	)
}